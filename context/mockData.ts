import { Project, Task, Contact, Invoice, User, ProjectStatus, InvoiceStatus, Priority, Opportunity, Expense, KnowledgeArticle, TeamPost, WebForm } from '../types';

// Empty arrays to ensure clean start
export const mockUsers: User[] = [];
export const mockProjects: Project[] = [];
export const mockTasks: Task[] = [];
export const mockContacts: Contact[] = [];
export const mockInvoices: Invoice[] = [];